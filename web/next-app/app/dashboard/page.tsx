import { AnalystPortal } from "@/components/portalViews";

/** Renders the interactive analyst workspace separately from the product story. */
export default function DashboardPage() {
  return <AnalystPortal />;
}
